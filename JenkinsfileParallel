pipeline {
    // agent any 
    // parameters {
    //    booleanParam(name: 'PUSH', defaultValue: false, description: 'Push to github')
    // }
    node("master") {
        timeout(unit: 'SECONDS', time: 5) {
            stage("One") {
                sleep 10
                echo 'hello'
            }
        }
    }
}
