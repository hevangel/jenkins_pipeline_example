pipeline {
    agent any
    triggers { 
        upstream(upstreamProjects: 'jenkins_pipeline_example_upstream, jenkins_pipeline_example_parallel', 
                threshold: hudson.model.Result.SUCCESS) 
    }
    stages {
        stage("Downstream") {
            println('hello world')
            steps {
                echo "${BUILD_TAG}"
            }
        }
    }
}
