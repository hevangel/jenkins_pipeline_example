pipeline {
    agent any
    stages {
        stage("Downstream") {
            steps {
                echo "${BUILD_TAG}"
                copyArtifacts projectName: 'upstream', selector: upstream()
                sh "cat upstream_artifact.txt"
            }
        }
    }
}
