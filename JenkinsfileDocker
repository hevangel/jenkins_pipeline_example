pipeline {
    agent {
        node {
            label 'master'
            docker {image 'ubuntu:latest'}
        }
    }
    stages {
        stage("Test") {
            steps {
                sh 'echo hello'
            }
        }
    }
}
